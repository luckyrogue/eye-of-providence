:8080 {
    root * /srv
    encode gzip zstd
    try_files {path} /index.html
    file_server
    header {
        # Privacy: не даём cache контролировать аналитику
        X-Frame-Options DENY
        X-Content-Type-Options nosniff
        Referrer-Policy strict-origin-when-cross-origin
    }
}
