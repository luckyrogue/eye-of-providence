// v2: новый dashboard с artifact-design (Eye of Providence (1)).
// Старый dashboard (./dashboard.tsx) пока остаётся в репо без use'а —
// будет удалён когда v2 пройдёт ревью + полностью wired'нется к API.
export { DashboardRoute } from "./v2";
